**sudo apt install
gcc make # before guest installation
vim
tmux
git

curl
nmap
netcat

docker.io
**thesleuthkit
**ghidra
download zip from website
install jdk dependency
https://jdk.java.net/

**clone dotfiles for vim and tmux
https://github.com/sko9370/dotfiles.git

**tmux
plugin manager: git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm 
sudo apt install xsel
tmux-yank
follow rest of directions from github above

**pwndbg
git clone https://github.com/pwndbg/pwndbg

**setup shared folder
sudo usermod -aG vboxsf $(whoami)
automount mounts to /media/sf_VMShared

**install 32 bit support for binaries
dpkg --add-architecture i386
apt-get update
apt-get install libc6:i386 libstdc++6:i386

**spotify-download
https://github.com/ritiek/spotify-downloader
use dockerfile inside repo
    docker build .
docker run -s [link]
sudo usermod -aG docker $USER
then relogin
